---
---

# الفصل الثالث: أساسيات البرمجة
**لغة Python للمعلوماتية الحيوية**

> **المفهوم الأساسي**: في الفصل الأول تعلمت كيف تصبح البيانات البيولوجية أرقاماً ونصوصاً. في الفصل الثاني تعلمت التعامل مع هذه الملفات عبر سطر الأوامر. الآن حان وقت السؤال: ماذا لو أردت تحليل 10,000 تسلسل بدلاً من واحد؟ هنا تأتي البرمجة—لتحويل الخطوات اليدوية إلى عمليات تلقائية قابلة للتكرار.

---

## نظرة عامة على الفصل

**المدة**: 3 أسابيع
**المتطلبات الأساسية**: الفصل 1 (البيانات الحيوية)، الفصل 2 (أساسيات Linux)
**مستوى البرمجة**: مناسب للمبتدئين (لا يلزم خبرة سابقة)

### أهداف التعلم

بإكمال هذا الفصل، ستكون قادراً على:

1. ✓ فهم المفاهيم الأساسية في Python: المتغيرات variables ، الأنواع types ، الشروط conditions، والحلقات loops
2. ✓ كتابة برامج Python للتعامل مع التسلسلات البيولوجية, برمجيات بسيطة
3. ✓ استخدام مكتبة Biopython للمهام الشائعة في المعلوماتية الحيوية، مكتبة مهمة للتعامل مع البيانات البيولوجية
4. ✓ تحليل ملفات بتنسيقات FASTA و FASTQ و GenBank 
5. ✓ الوصول إلى قواعد بيانات NCBI برمجياً باستخدام Entrez 
6. ✓ كتابة نصوص برمجية قابلة لإعادة الاستخدام وموثقة جيداً (دوال ووظائف)

---

## 1. لماذا Python؟

### المشكلة التي نحلها

ضع نفسك في التصور التالي: تخيل أنك في المختبر وحصلت على نتائج تسلسل جينومي لـ 50 عينة. تحتاج أن:

- تتحقق من جودة كل تسلسل 
- تحسب نسبة GC لكل عينة 
- تقارن النتائج وتصنفها 
- تكتب تقريراً بالنتائج مبسط

يدوياً؟ ستحتاج أياماً. ببرنامج Python؟ دقائق.

(طبعاً هذا تبسيط، الأصل أن تصمم التجربة التي تولّد البيانات التي تحتاج للإجابة على السؤال)
### لماذا Python تحديداً؟

هناك لغات برمجة كثيرة، لكن Python تتميز في المعلوماتية الحيوية لأسباب عملية:

| الميزة           | لماذا تهمك                                                                     |
|------------------|--------------------------------------------------------------------------------|
| **بناء جملة واضح** | الكود يُقرأ كأنه تعليمات عادية، مما يقلل الأخطاء (قريب لمفهوم الكود الوهمي psudocode) |
| **مكتبات متخصصة** | Biopython و NumPy و Pandas جاهزة للاستخدام                                     |
| **تفاعلية**      | يمكنك تجربة الأفكار سطراً بسطر في Jupyter   (لغة تأويل سطر بسطر وليس لانتاج ملفات برامج) |
| **مجتمع كبير**   | أي سؤال تواجهه، غالباً أجاب عنه شخص قبلك                                        |

!!! info "مقارنة سريعة باللغات الأخرى"
    - **R**: أقوى في الإحصاء والرسوم البيانية، لكن أصعب في معالجة الملفات
    - **Perl**: كانت لغة المعلوماتية الحيوية في التسعينيات، الآن يُستبدل بها Python
    - **Bash**: ممتاز للمهام البسيطة (تعلمته في الفصل 2)، لكن محدود للتحليل المعقد
    - **Python**: التوازن الأفضل—سهل التعلم، قوي في التحليل، مكتبات غنية

### إعداد بيئة العمل

**ابحث في الانترنت عن تثبيت بايثون والافضل في بيئة قابلة للتعديل مثل conda**
قبل أن نبدأ، تأكد من تثبيت Python:

```bash
# التحقق من الإصدار
python3 --version
# يجب أن يظهر Python 3.9 أو أحدث

# تثبيت Biopython (سنحتاجه لاحقاً)
pip install biopython
```

لتشغيل أمثلة هذا الفصل، يمكنك استخدام:

- **الطريقة التفاعلية**: اكتب `python3` في الطرفية للدخول إلى المفسر التفاعلي
- **ملف برمجي**: اكتب الكود في ملف `.py` وشغّله بـ `python3 filename.py`
- **Jupyter Notebook**: الأفضل للتعلم—يتيح تشغيل الكود خلية بخلية

---

## 2. أساسيات Python

### 2.1 المتغيرات: تخزين المعلومات

#### لماذا نحتاج المتغيرات؟

عندما تحلل تسلسلاً جينياً، تحتاج مكاناً تحفظ فيه التسلسل، طوله، اسم الجين، نسبة GC... كل هذه قيم تحتاج أن تُخزَّن وتُسترجع. المتغير هو ببساطة **اسم تعطيه لقيمة معينة** حتى تستخدمها لاحقاً.
 مثل س في الرياضيات فهي خانة لرقم متغير، لكن في بايثون لتخزين قيمة والعودة لها. 
 
#### المفهوم الأساسي

```python
# المتغير = اسم يشير إلى قيمة
gene_name = "BRCA1"
```

السطر أعلاه يقول: "خزّن النص `BRCA1` تحت الاسم `gene_name`". الآن يمكنك استخدام `gene_name` في أي مكان بدلاً من كتابة `"BRCA1"` كل مرة. في كل مرة تذكر المتغير فأنت تذكر الجين. 

#### أنواع البيانات الأساسية

كل قيمة في Python لها **نوع** يحدد ما يمكنك فعله بها:

```python
# نص (str) — لتخزين الأسماء والتسلسلات
gene_name = "BRCA1"
dna_sequence = "ATGCGA"

# عدد صحيح (int) — للعد والفهرسة
sequence_length = 5589
num_exons = 23

# عدد عشري (float) — للقياسات والنسب
gc_content = 55.6
p_value = 0.003

# قيمة منطقية (bool) — للأسئلة بنعم/لا
is_coding = True
has_mutation = False
```

!!! tip "قواعد تسمية المتغيرات"
    - استخدم أسماء **وصفية**: `gene_name` أفضل بكثير من `g` أو `x`
    - افصل الكلمات بشرطة سفلية: `gc_content` وليس `gccontent`
    - لا تبدأ الاسم برقم: `seq1` صحيح، `1seq` خطأ
    - تجنب الأسماء المحجوزة: لا تسمِّ متغيراً `print` أو `list`

#### تجربة عملية

```python
# لنخزن معلومات عن جين TP53
gene_name = "TP53"
chromosome = 17
length_bp = 19149
is_tumor_suppressor = True

# يمكننا طباعة هذه المعلومات
print(gene_name)              # TP53
print(length_bp)              # 19149
print(is_tumor_suppressor)    # True

# يمكننا أيضاً دمجها في جملة
print(f"الجين {gene_name} يقع على الكروموسوم {chromosome}")
# الإخراج: الجين TP53 يقع على الكروموسوم 17
```

دالة `print()` تعرض القيمة على الشاشة. حرف `f` قبل النص يعني "formatted string" — يسمح لك بإدراج قيم المتغيرات داخل النص باستخدام الأقواس المعقوفة `{}`.

---

### 2.2 النصوص: التعامل مع التسلسلات

#### لماذا النصوص مهمة؟

تسلسلات DNA و RNA والبروتين هي **نصوص** بطبيعتها — سلسلة من الحروف. تسلسل DNA مثل `ATGCGATCG` هو نص مكون من 9 أحرف. لذلك فهم كيفية التعامل مع النصوص في Python يعني فهم كيفية التعامل مع التسلسلات البيولوجية.

#### العمليات الأساسية على النصوص

**طول النص — كم نيوكليوتيد في التسلسل؟**

```python
dna = "ATGCGATCG"
print(len(dna))  # 9
```

دالة `len()` تُرجع عدد الحروف. في السياق البيولوجي، هذا يعني عدد القواعد النيتروجينية (bp).

**التقطيع — استخراج جزء من التسلسل:**

```python
dna = "ATGCGATCGTAGC"

# أول 3 حروف (الكودون الأول)
first_codon = dna[0:3]
print(first_codon)  # ATG

# ثاني كودون
second_codon = dna[3:6]
print(second_codon)  # CGA

# آخر 3 حروف
last_codon = dna[-3:]
print(last_codon)  # AGC
```

!!! warning "الفهرسة تبدأ من الصفر"
    في Python، الحرف الأول ترتيبه `0` وليس `1`. هذا مربك في البداية لكنه اتفاقية ثابتة:

    ```
    التسلسل:  A  T  G  C  G  A
    الفهرس:   0  1  2  3  4  5
    ```

    لذلك `dna[0]` تعطيك `A`، و `dna[0:3]` تعطيك الحروف من الفهرس 0 إلى 2 (الرقم الأخير لا يُضمَّن).

**العد — كم مرة يظهر حرف معين؟**

```python
dna = "ATGCGATCGTAGC"

# عد كل نيوكليوتيد
a_count = dna.count('A')  # 3
t_count = dna.count('T')  # 2
g_count = dna.count('G')  # 4
c_count = dna.count('C')  # 3

print(f"A={a_count}, T={t_count}, G={g_count}, C={c_count}")
```

**الاستبدال — تحويل DNA إلى RNA:**

```python
dna = "ATGCGATCG"

# النسخ: استبدال T بـ U
rna = dna.replace('T', 'U')
print(rna)  # AUGCGAUCG
```

`.replace()` تنشئ نسخة جديدة من النص مع الاستبدال — النص الأصلي لا يتغير.

#### تطبيق عملي: حساب نسبة GC

نسبة GC (GC content) هي من أهم المقاييس الأساسية في تحليل التسلسلات. تخبرنا عن استقرار الـ DNA لأن الروابط بين G و C أقوى (3 روابط هيدروجينية) من الروابط بين A و T (رابطتان).

```python
dna = "ATGCGATCGTAGC"

# الخطوة 1: عد G و C
g_count = dna.count('G')  # 4
c_count = dna.count('C')  # 3
gc_total = g_count + c_count  # 7

# الخطوة 2: حساب النسبة
total_bases = len(dna)  # 13
gc_percent = (gc_total / total_bases) * 100

print(f"GC content: {gc_percent:.1f}%")  # GC content: 53.8%
```

`:.1f` في السطر الأخير يعني "اعرض رقماً عشرياً واحداً بعد النقطة".

---

### 2.3 القوائم: إدارة مجموعات البيانات lists 

#### لماذا نحتاج القوائم؟

متغير واحد يخزن قيمة واحدة. لكن في المعلوماتية الحيوية نادراً ما نعمل على تسلسل واحد — عادةً لدينا عشرات أو مئات أو آلاف التسلسلات. القائمة (list) تتيح لك تخزين **مجموعة مرتبة من القيم** في متغير واحد.

#### إنشاء القوائم

```python
# قائمة بأسماء جينات
genes = ["BRCA1", "TP53", "EGFR", "MYC"]

# قائمة بقيم تعبير جيني
expression_values = [145.3, 523.8, 189.2, 856.1]

# قائمة بأطوال تسلسلات
seq_lengths = [5589, 19149, 1210, 4857]

# حتى قائمة فارغة (سنملؤها لاحقاً)
results = []
```

#### الوصول إلى عناصر القائمة

```python
genes = ["BRCA1", "TP53", "EGFR", "MYC"]

# العنصر الأول (الفهرسة تبدأ من 0)
print(genes[0])   # BRCA1

# العنصر الأخير
print(genes[-1])  # MYC

# عدد العناصر
print(len(genes)) # 4
```

#### تعديل القوائم

```python
genes = ["BRCA1", "TP53", "EGFR"]

# إضافة عنصر في النهاية
genes.append("MYC")
print(genes)  # ["BRCA1", "TP53", "EGFR", "MYC"]

# إضافة عنصر آخر
genes.append("KRAS")
print(genes)  # ["BRCA1", "TP53", "EGFR", "MYC", "KRAS"]

# الترتيب أبجدياً
genes.sort()
print(genes)  # ["BRCA1", "EGFR", "KRAS", "MYC", "TP53"]

# التحقق من وجود عنصر
print("TP53" in genes)   # True
print("BRAF" in genes)   # False
```

#### تطبيق عملي: إحصائيات بسيطة على بيانات التعبير الجيني gene expression 

```python
expression = [145.3, 523.8, 189.2, 856.1, 312.5]

# الحد الأدنى والأقصى
print(f"أقل قيمة: {min(expression)}")   # 145.3
print(f"أعلى قيمة: {max(expression)}")  # 856.1

# المتوسط الحسابي
mean_expr = sum(expression) / len(expression)
print(f"المتوسط: {mean_expr:.1f}")  # 405.4

# عدد الجينات عالية التعبير (أعلى من 400)
high_expr = [val for val in expression if val > 400]
print(f"عدد الجينات عالية التعبير: {len(high_expr)}")  # 2
```

السطر الأخير يستخدم ما يسمى **List Comprehension** — طريقة مختصرة لتصفية القوائم. اقرأه كالتالي: "أعطني كل `val` من `expression` بشرط أن `val` أكبر من 400".

---

### 2.4 القواميس: ربط المعلومات ببعضها

#### لماذا نحتاج القواميس؟

لديك قائمة جينات وقائمة قيم تعبير — كيف تعرف أي قيمة تخص أي جين؟ يمكنك استخدام قائمتين متوازيتين، لكن هذا عرضة للأخطاء. القاموس (dictionary) يربط كل **مفتاح** بـ **قيمة** مباشرة — تماماً كربط اسم الجين بقيمة تعبيره.

#### إنشاء القواميس

```python
# ربط كل جين بقيمة تعبيره
gene_expression = {
    "BRCA1": 145.3,
    "TP53": 523.8,
    "EGFR": 189.2,
    "MYC": 856.1
}
```

البنية واضحة: `"المفتاح": القيمة`. كل جين (المفتاح) مرتبط بقيمة تعبير واحدة (القيمة).

#### الوصول والتعديل

```python
# الوصول إلى قيمة عبر المفتاح
brca1_expr = gene_expression["BRCA1"]
print(brca1_expr)  # 145.3

# إضافة إدخال جديد
gene_expression["KRAS"] = 234.5

# التحقق من وجود جين
if "TP53" in gene_expression:
    print(f"TP53 expression: {gene_expression['TP53']}")
# الإخراج: TP53 expression: 523.8

# ماذا لو بحثت عن جين غير موجود؟
# gene_expression["BRAF"]  # ❌ KeyError!
# الطريقة الآمنة:
braf_expr = gene_expression.get("BRAF", 0)  # يُرجع 0 إذا لم يُوجد
print(braf_expr)  # 0
```

#### تطبيق عملي: جدول الكودونات

الكودون هو 3 نيوكليوتيدات تُترجم إلى حمض أميني واحد. هذه العلاقة طبيعياً يمكن تمثليها بقاموس:

```python
# جدول الكودونات (مختصر)
codon_table = {
    "ATG": "Met",  # كودون البداية
    "TAA": "Stop", "TAG": "Stop", "TGA": "Stop",  # كودونات التوقف
    "GCT": "Ala", "GCC": "Ala", "GCA": "Ala",     # Alanine
    "TGT": "Cys", "TGC": "Cys",                     # Cysteine
    "GAT": "Asp", "GAC": "Asp",                     # Aspartic acid
    "TTT": "Phe", "TTC": "Phe",                     # Phenylalanine
}

# ترجمة كودون واحد
codon = "ATG"
amino_acid = codon_table[codon]
print(f"{codon} → {amino_acid}")  # ATG → Met

# ترجمة تسلسل (كل 3 حروف)
dna = "ATGGCTGAT"
protein = []
for i in range(0, len(dna), 3):        # 0, 3, 6
    codon = dna[i:i+3]                  # ATG, GCT, GAT
    amino_acid = codon_table.get(codon, "?")
    protein.append(amino_acid)

print(" - ".join(protein))  # Met - Ala - Asp
```

---

### 2.5 الشروط: اتخاذ قرارات بناءً على البيانات

#### لماذا نحتاج الشروط؟

تحليل البيانات البيولوجية يتطلب اتخاذ قرارات: هل هذا التسلسل طويل بما يكفي؟ هل نسبة GC طبيعية؟ هل جودة القراءة مقبولة؟ الشروط تسمح للبرنامج باتخاذ قرارات مختلفة بناءً على البيانات.

#### البنية الأساسية

```python
gc_percent = 65

if gc_percent > 60:
    print("نسبة GC عالية")
elif gc_percent > 40:
    print("نسبة GC متوسطة")
else:
    print("نسبة GC منخفضة")

# الإخراج: نسبة GC عالية
```

- `if` تتحقق من الشرط الأول
- `elif` (اختصار else if) تتحقق من شرط بديل
- `else` تُنفَّذ إذا لم يتحقق أي شرط سابق
- **المسافة البادئة (4 مسافات) ضرورية** — هي التي تحدد أي أسطر تتبع أي شرط

#### عوامل المقارنة

```python
x = 10

x == 10    # True — هل يساوي؟
x != 5     # True — هل لا يساوي؟
x > 5      # True — هل أكبر من؟
x >= 10    # True — هل أكبر من أو يساوي؟
x < 20     # True — هل أصغر من؟

# الدمج بين شروط
x > 5 and x < 20   # True — كلا الشرطين
x > 5 or x > 100   # True — أحد الشرطين على الأقل
not (x > 100)       # True — عكس الشرط
```

#### تطبيق: تصنيف التسلسلات حسب محتوى GC

```python
def classify_gc(sequence):
    """تصنيف تسلسل DNA حسب نسبة GC."""
    gc_count = sequence.count('G') + sequence.count('C')
    gc_percent = (gc_count / len(sequence)) * 100

    if gc_percent < 30:
        return "AT-rich (منخفض GC)"
    elif gc_percent < 45:
        return "AT-leaning (مائل لـ AT)"
    elif gc_percent < 55:
        return "Balanced (متوازن)"
    elif gc_percent < 70:
        return "GC-leaning (مائل لـ GC)"
    else:
        return "GC-rich (عالي GC)"

# أمثلة
print(classify_gc("AAATTTAAAT"))   # AT-rich (منخفض GC)
print(classify_gc("ATGCGATCGA"))   # GC-leaning (مائل لـ GC)
print(classify_gc("GCGCGCGCGC"))   # GC-rich (عالي GC)
```

!!! info "لماذا تهمنا نسبة GC؟"
    - الكائنات الحية لها نسب GC مميزة (مثلاً: *E. coli* ~51%، *Plasmodium* ~20%)
    - المناطق الغنية بـ GC في الجينوم البشري غالباً تحتوي على كثافة أعلى من الجينات
    - نسبة GC تؤثر على درجة حرارة الانصهار (Tm) للـ DNA
    - نقل الجينات الأفقي يُكتشف أحياناً عبر اختلاف نسبة GC

---

### 2.6 الحلقات: تكرار العمليات

#### لماذا نحتاج الحلقات؟

القوة الحقيقية للبرمجة تكمن في **التكرار**. بدلاً من كتابة نفس الكود 1000 مرة لتحليل 1000 تسلسل، تكتبه مرة واحدة وتطلب من الحاسوب تكراره.

#### حلقة for: التكرار عبر مجموعة

```python
# أبسط مثال: طباعة كل جين
genes = ["BRCA1", "TP53", "EGFR"]

for gene in genes:
    print(gene)

# الإخراج:
# BRCA1
# TP53
# EGFR
```

اقرأها: "لكل `gene` في قائمة `genes`، نفّذ ما بداخل الحلقة".

**التكرار مع معالجة:**

```python
sequences = ["ATGCGT", "GCGCGC", "ATATAT"]

for seq in sequences:
    gc = (seq.count('G') + seq.count('C')) / len(seq) * 100
    print(f"{seq}: GC = {gc:.1f}%")

# الإخراج:
# ATGCGT: GC = 50.0%
# GCGCGC: GC = 100.0%
# ATATAT: GC = 0.0%
```

**التكرار بالأرقام:**

```python
# range(start, stop, step)
# تقطيع تسلسل إلى كودونات (كل 3 حروف)
dna = "ATGGCTGATCGA"

for i in range(0, len(dna), 3):
    codon = dna[i:i+3]
    print(f"Position {i}: {codon}")

# الإخراج:
# Position 0: ATG
# Position 3: GCT
# Position 6: GAT
# Position 9: CGA
```

`range(0, 12, 3)` تولد الأرقام: 0، 3، 6، 9.

#### حلقة while: التكرار حتى يتحقق شرط

```python
# البحث عن كودون التوقف في تسلسل
dna = "ATGGCTGATCGATAAGTC"
stop_codons = ["TAA", "TAG", "TGA"]

position = 0
while position < len(dna) - 2:
    codon = dna[position:position+3]
    if codon in stop_codons:
        print(f"كودون توقف {codon} في الموقع {position}")
        break  # خروج من الحلقة
    position += 3

# الإخراج: كودون توقف TAA في الموقع 12
```

#### تطبيق عملي: تحليل مجموعة تسلسلات

```python
# مجموعة تسلسلات من تجربة
sequences = {
    "sample_01": "ATGCGATCGTAGCTAGCTGATCG",
    "sample_02": "AAATTTAAATTTAAAGGG",
    "sample_03": "GCGCGCGCGCGCATGCGC",
    "sample_04": "ATATATCGCGATATATAT",
    "sample_05": "GGGCCCGGGCCCGGGCCC"
}

print(f"{'العينة':<12} {'الطول':>6} {'GC%':>6} {'التصنيف'}")
print("-" * 50)

for name, seq in sequences.items():
    length = len(seq)
    gc = (seq.count('G') + seq.count('C')) / length * 100
    classification = classify_gc(seq)  # الدالة التي كتبناها سابقاً
    print(f"{name:<12} {length:>6} {gc:>5.1f}% {classification}")
```

`sequences.items()` تعطينا كل **مفتاح وقيمته** معاً في كل دورة من الحلقة.

---

### 2.7 الدوال: بناء أدوات قابلة لإعادة الاستخدام

#### لماذا نحتاج الدوال؟

لاحظ أننا حسبنا نسبة GC أكثر من مرة بنفس الطريقة. بدلاً من نسخ نفس الأسطر كل مرة، نجمعها في **دالة** — مجموعة من الكود المسمى يمكنك استدعاؤه في أي وقت.

الدوال تحقق ثلاثة أهداف:

1. **عدم التكرار** — اكتب الكود مرة واحدة، استخدمه مراراً
2. **الوضوح** — `calculate_gc(seq)` أوضح من 4 أسطر حسابية
3. **الموثوقية** — إذا وجدت خطأ، تصلحه في مكان واحد

#### بنية الدالة

```python
def calculate_gc(sequence):
    """حساب نسبة GC لتسلسل DNA."""
    gc_count = sequence.count('G') + sequence.count('C')
    return (gc_count / len(sequence)) * 100
```

- `def` تعني "عرّف دالة جديدة"
- `calculate_gc` اسم الدالة
- `sequence` هو **المعامل** — القيمة التي تدخل إلى الدالة
- النص بين `"""` يسمى **docstring** — يشرح ما تفعله الدالة
- `return` تُرجع النتيجة

**الاستخدام:**

```python
# استدعاء الدالة مع تسلسلات مختلفة
result1 = calculate_gc("ATGCGATCG")
result2 = calculate_gc("AAATTTAAA")
result3 = calculate_gc("GCGCGCGCG")

print(f"التسلسل 1: {result1:.1f}%")  # 55.6%
print(f"التسلسل 2: {result2:.1f}%")  # 0.0%
print(f"التسلسل 3: {result3:.1f}%")  # 100.0%
```

#### دالة أكثر تعقيداً: المتمم العكسي

المتمم العكسي (reverse complement) هو عملية أساسية في البيولوجيا الجزيئية. الشريط المقابل لـ DNA يُقرأ في الاتجاه المعاكس مع استبدال كل قاعدة بمتممها:

- A ↔ T
- G ↔ C

```python
def reverse_complement(dna_seq):
    """
    حساب المتمم العكسي لتسلسل DNA.

    المتمم العكسي مهم لأن DNA مزدوج الشريط،
    والشريط المقابل يُقرأ في الاتجاه المعاكس (3' إلى 5') لا تنسى تجريد الأحياء والرقمنة انعكاس للحقيقة.

    Args:
        dna_seq (str): تسلسل DNA يحتوي على A, T, G, C

    Returns:
        str: تسلسل المتمم العكسي

    Example:
        >>> reverse_complement("ATGC")
        'GCAT'
    """
    # الخطوة 1: تعريف جدول المتممات
    complement = {'A': 'T', 'T': 'A', 'G': 'C', 'C': 'G'}

    # الخطوة 2: استبدال كل قاعدة بمتممها
    comp_seq = ""
    for base in dna_seq:
        comp_seq = comp_seq + complement[base]

    # الخطوة 3: عكس الاتجاه
    rev_comp = comp_seq[::-1]

    return rev_comp

# اختبار
original = "ATGCGATCG"
result = reverse_complement(original)
print(f"الأصلي:        5'-{original}-3'")
print(f"المتمم العكسي: 3'-{result}-5'")
# الأصلي:        5'-ATGCGATCG-3'
# المتمم العكسي: 3'-CGATCGCAT-5'
```

!!! tip "مبادئ كتابة دوال جيدة"
    1. **غرض واحد** — كل دالة تؤدي مهمة واحدة محددة
    2. **اسم وصفي** — `reverse_complement` أوضح بكثير من `rc`
    3. **توثيق** — اكتب docstring يشرح ما تفعله الدالة ولماذا
    4. **اختبار** — تأكد أن الدالة تعطي نتائج صحيحة مع مدخلات معروفة

#### دالة شاملة: إحصائيات التسلسل

لنجمع ما تعلمناه في دالة واحدة مفيدة:

```python
def sequence_stats(dna_seq):
    """
    حساب إحصائيات شاملة لتسلسل DNA.

    Args:
        dna_seq (str): تسلسل DNA

    Returns:
        dict: قاموس يحتوي على الإحصائيات
    """
    length = len(dna_seq)

    # عد كل نيوكليوتيد
    counts = {
        'A': dna_seq.count('A'),
        'T': dna_seq.count('T'),
        'G': dna_seq.count('G'),
        'C': dna_seq.count('C')
    }

    # نسبة GC
    gc_content = (counts['G'] + counts['C']) / length * 100

    # نسبة AT
    at_content = (counts['A'] + counts['T']) / length * 100

    return {
        'length': length,
        'gc_content': round(gc_content, 2),
        'at_content': round(at_content, 2),
        'nucleotide_counts': counts
    }

# استخدام
stats = sequence_stats("ATGCGATCGTAGCTAGCT")
print(f"الطول: {stats['length']} bp")
print(f"محتوى GC: {stats['gc_content']}%")
print(f"محتوى AT: {stats['at_content']}%")
print(f"عدد النيوكليوتيدات: {stats['nucleotide_counts']}")
```

---

**عذرا على الاطالة ، يمكن عمل كتيب نوت بووك عملي لاحقاً**

## 3. Biopython: أدوات متخصصة للمعلوماتية الحيوية

### لماذا لا تكفي Python وحدها؟

في القسم السابق كتبنا دوال مثل `reverse_complement` و `calculate_gc` يدوياً. هذا مفيد للتعلم، لكن في العمل الحقيقي:

- ستحتاج عشرات الدوال المتشابهة — هل ستكتبها كلها من الصفر؟
- ستحتاج قراءة ملفات بتنسيقات مختلفة — FASTA, FASTQ, GenBank...
- ستحتاج الوصول إلى قواعد بيانات عبر الإنترنت

**Biopython** هي مكتبة مفتوحة المصدر توفر كل هذا جاهزاً. فكر فيها كصندوق أدوات — بدلاً من صنع مطرقة كل مرة، تفتح الصندوق وتستخدم المطرقة الجاهزة.

```bash
pip install biopython
```

### 3.1 كائن Seq: نص بقدرات بيولوجية

#### من نص عادي إلى كائن بيولوجي

```python
# باستخدام نص عادي — يعمل لكنه محدود
dna_string = "ATGCGATCG"
# dna_string.transcribe()  # ❌ خطأ! النصوص لا تعرف البيولوجيا

# باستخدام كائن Seq — يفهم البيولوجيا
from Bio.Seq import Seq

dna_seq = Seq("ATGCGATCG")
rna_seq = dna_seq.transcribe()    # ✓ النسخ
print(rna_seq)                     # AUGCGAUCG

rev_comp = dna_seq.reverse_complement()  # ✓ المتمم العكسي
print(rev_comp)                          # CGATCGCAT
```

كائن `Seq` يبدو كنص عادي ويدعم نفس العمليات (التقطيع، العد، البحث)، لكنه يضيف **طرقاً بيولوجية** مثل النسخ والترجمة والمتمم.

#### العمليات البيولوجية الأساسية

**النسخ والترجمة — المبدأ المركزي central dogma:**

```python
from Bio.Seq import Seq

# تسلسل DNA
dna = Seq("ATGAAAGCTTGA")
print(f"DNA:     {dna}")       # ATGAAAGCTTGA

# الخطوة 1: النسخ (DNA → RNA)
rna = dna.transcribe()
print(f"RNA:     {rna}")       # AUGAAAGCUUGA

# الخطوة 2: الترجمة (RNA → بروتين)
protein = rna.translate()
print(f"Protein: {protein}")   # MKA*
```

`*` يمثل كودون التوقف. الحمض الأميني `M` (ميثيونين) هو دائماً البداية.

!!! note "يمكنك الترجمة مباشرة من DNA"
    ```python
    protein = dna.translate()  # يعمل أيضاً — Biopython يتعامل مع الخطوات الوسيطة
    ```

**عمليات مشتركة مع النصوص:**

```python
from Bio.Seq import Seq

seq = Seq("ATGCGATCGTAGCT")

# كل ما تعرفه عن النصوص يعمل هنا أيضاً
print(len(seq))           # 14 — الطول
print(seq[0:3])           # ATG — التقطيع
print(seq.count('G'))     # 4 — العد
print(seq.find("TCG"))    # 6 — البحث عن نمط

# حساب نسبة GC
gc = (seq.count('G') + seq.count('C')) / len(seq) * 100
print(f"GC: {gc:.1f}%")  # GC: 57.1%
```

#### تطبيق عملي: تحليل إطار القراءة المفتوح (ORF)

إطار القراءة المفتوح هو المنطقة من كودون البداية (ATG) إلى كودون التوقف. الجينات الحقيقية تقع ضمن ORFs:

```python
from Bio.Seq import Seq

dna = Seq("TTTATGAAAGCTGATCGTTGAAACCC")

# البحث عن كودون البداية
start_pos = dna.find("ATG")
print(f"كودون البداية في الموقع: {start_pos}")  # 3

# استخراج التسلسل بدءاً من ATG
orf_region = dna[start_pos:]
print(f"المنطقة: {orf_region}")  # ATGAAAGCTGATCGTTGAAACCC

# ترجمة حتى أول كودون توقف
protein = orf_region.translate()
print(f"البروتين: {protein}")  # MKA*RLKP  (نلاحظ * = كودون توقف)

# الترجمة مع التوقف عند أول كودون توقف
protein_to_stop = orf_region.translate(to_stop=True)
print(f"البروتين (حتى التوقف): {protein_to_stop}")  # MKA
```

---

## 4. العمل مع تنسيقات الملفات البيولوجية

### لماذا تنسيقات مختلفة؟

في الفصل الأول تعلمنا أن البيانات البيولوجية متنوعة. كل نوع بيانات يحتاج تنسيق يناسبه:

| التنسيق     | ماذا يحتوي           | متى يُستخدم                 |
|-------------|----------------------|----------------------------|
| **FASTA**   | التسلسل فقط + معرف   | مشاركة التسلسلات، قواعد البيانات |
| **FASTQ**   | التسلسل + جودة كل قاعدة | بيانات أجهزة التسلسل مباشرة |
| **GenBank** | التسلسل + تعليقات تفصيلية | التسلسلات المرجعية المشروحة |

فكر فيها كمستويات من المعلومات:
**FASTA** (الأبسط) → **FASTQ** (+ جودة) → **GenBank** (+ تعليقات كاملة)

---

### 4.1 تنسيق FASTA

#### الهيكل

```
>seq_id وصف اختياري
ATGCGATCGTAGCTAGCTGATCGATCG
TCGATCGATCGTACGATCGATCGATCG
```

- السطر الأول يبدأ بـ `<` ويحتوي معرف التسلسل والوصف
- الأسطر التالية تحتوي التسلسل نفسه (قد يمتد على عدة أسطر)

#### قراءة ملف FASTA

```python
from Bio import SeqIO

# قراءة جميع التسلسلات في الملف
for record in SeqIO.parse("sequences.fasta", "fasta"):
    print(f"المعرف: {record.id}")
    print(f"الوصف: {record.description}")
    print(f"التسلسل: {record.seq}")
    print(f"الطول: {len(record)} bp")
    print("---")
```

`SeqIO.parse()` تقرأ الملف تسلسلاً تسلسلاً. كل `record` يحتوي على:

- `record.id` — معرف التسلسل (الكلمة الأولى بعد `>`)
- `record.description` — السطر الكامل بعد `>`
- `record.seq` — كائن `Seq` يحتوي التسلسل
- `len(record)` — طول التسلسل

#### تطبيق تدريجي: من القراءة إلى التحليل

**المستوى 1: قراءة وعرض المعلومات الأساسية**

```python
from Bio import SeqIO

# عد التسلسلات وأطوالها
count = 0
total_length = 0

for record in SeqIO.parse("sequences.fasta", "fasta"):
    count += 1
    total_length += len(record)

print(f"عدد التسلسلات: {count}")
print(f"إجمالي القواعد: {total_length}")
print(f"متوسط الطول: {total_length / count:.0f} bp")
```

**المستوى 2: تصفية التسلسلات حسب الطول**

```python
from Bio import SeqIO

def filter_by_length(input_file, output_file, min_length):
    """
    الاحتفاظ بالتسلسلات الأطول من حد أدنى معين.

    يُستخدم مثلاً لإزالة القراءات القصيرة جداً التي قد تكون
    نتيجة أخطاء في التسلسل أو تلوث.
    """
    kept = 0
    removed = 0

    # جمع التسلسلات التي تمر المعيار
    long_sequences = []
    for record in SeqIO.parse(input_file, "fasta"):
        if len(record.seq) >= min_length:
            long_sequences.append(record)
            kept += 1
        else:
            removed += 1

    # كتابة النتائج
    SeqIO.write(long_sequences, output_file, "fasta")
    print(f"تم الاحتفاظ بـ {kept} تسلسل (≥ {min_length} bp)")
    print(f"تم إزالة {removed} تسلسل قصير")

# مثال
filter_by_length("all_seqs.fasta", "long_seqs.fasta", min_length=500)
```

**المستوى 3: تحليل وتقرير**

```python
from Bio import SeqIO

def analyze_fasta(input_file):
    """تحليل شامل لملف FASTA مع تقرير."""
    stats = []

    for record in SeqIO.parse(input_file, "fasta"):
        seq = str(record.seq)
        gc = (seq.count('G') + seq.count('C')) / len(seq) * 100
        stats.append({
            'id': record.id,
            'length': len(seq),
            'gc': gc
        })

    # طباعة تقرير
    print(f"\n{'='*60}")
    print(f"  تقرير تحليل: {input_file}")
    print(f"{'='*60}")
    print(f"  عدد التسلسلات: {len(stats)}")

    lengths = [s['length'] for s in stats]
    print(f"  أقصر تسلسل: {min(lengths)} bp")
    print(f"  أطول تسلسل: {max(lengths)} bp")
    print(f"  متوسط الطول: {sum(lengths)/len(lengths):.0f} bp")

    gc_values = [s['gc'] for s in stats]
    print(f"  متوسط GC: {sum(gc_values)/len(gc_values):.1f}%")
    print(f"{'='*60}\n")

    # طباعة تفاصيل كل تسلسل
    print(f"{'المعرف':<20} {'الطول':>8} {'GC%':>8}")
    print("-" * 40)
    for s in stats:
        print(f"{s['id']:<20} {s['length']:>8} {s['gc']:>7.1f}%")

# مثال
analyze_fasta("sequences.fasta")
```

---

### 4.2 تنسيق FASTQ: التسلسل مع معلومات الجودة

#### لماذا نحتاج الجودة؟

أجهزة التسلسل ليست مثالية — كل قاعدة تُقرأ لها **درجة ثقة** وهي تعبير لجودة تحويل التسلسل إلى إشارة ضوئية ترمز للقاعدة النتروجينية. تنسيق FASTQ يضيف هذه المعلومة:

```
@seq_id
ATGCGATCGTAGCT
+
IIIHHGGGFFFEEE
```

- السطر 1: المعرف (يبدأ بـ `@`)
- السطر 2: التسلسل
- السطر 3: `+` (فاصل)
- السطر 4: **درجات الجودة** — حرف لكل قاعدة

#### فهم درجات الجودة (Phred Scores)

كل حرف في سطر الجودة يمثل رقماً يعبر عن دقة القراءة:

| الحرف | درجة Phred | دقة القراءة | معنى    |
|-------|------------|-----------|---------|
| `I`   | 40         | 99.99%    | ممتاز   |
| `F`   | 37         | 99.98%    | جيد جداً |
| `5`   | 20         | 99%       | مقبول   |
| `!`   | 0          | لا يوجد   | غير موثوق |

!!! info "القاعدة العامة"
    درجة Phred ≥ 30 تعني أن احتمال الخطأ أقل من 1 في 1000. هذا هو الحد الأدنى المقبول عادةً.

#### قراءة وتحليل ملفات FASTQ

**قراءة أساسية:**

```python
from Bio import SeqIO

for record in SeqIO.parse("reads.fastq", "fastq"):
    print(f"المعرف: {record.id}")
    print(f"التسلسل: {record.seq}")

    # درجات الجودة كأرقام
    qualities = record.letter_annotations["phred_quality"]
    print(f"الجودة: {qualities}")

    # حساب متوسط الجودة
    mean_quality = sum(qualities) / len(qualities)
    print(f"متوسط الجودة: {mean_quality:.1f}")
    print("---")
```

**تصفية القراءات حسب الجودة:**

```python
from Bio import SeqIO

def filter_by_quality(input_fastq, output_fastq, min_quality=30):
    """
    الاحتفاظ بالقراءات التي متوسط جودتها أعلى من الحد الأدنى.

    هذه خطوة أساسية في أي خط تحليل للتسلسل (pipeline).
    القراءات منخفضة الجودة تُدخل أخطاء في التحليلات اللاحقة.
    """
    total = 0
    kept = 0

    high_quality_reads = []

    for record in SeqIO.parse(input_fastq, "fastq"):
        total += 1
        qualities = record.letter_annotations["phred_quality"]
        mean_qual = sum(qualities) / len(qualities)

        if mean_qual >= min_quality:
            high_quality_reads.append(record)
            kept += 1

    SeqIO.write(high_quality_reads, output_fastq, "fastq")

    removed = total - kept
    print(f"الإجمالي: {total} قراءة")
    print(f"تم الاحتفاظ: {kept} ({kept/total*100:.1f}%)")
    print(f"تم إزالة: {removed} ({removed/total*100:.1f}%)")

# مثال
filter_by_quality("raw_reads.fastq", "filtered_reads.fastq", min_quality=30)
```

---

### 4.3 تنسيق GenBank: التسلسل المشروح بالكامل

#### لماذا GenBank مختلف؟

FASTA يعطيك التسلسل فقط. GenBank يعطيك **كل شيء عن التسلسل**:

- التسلسل نفسه
- الكائن الحي الذي ينتمي إليه
- مواقع الجينات ومناطق الترميز
- المراجع العلمية
- التعليقات التوضيحية

فكر في FASTA كنص خام، و GenBank ككتاب مع فهرس وهوامش وحواشي.

#### قراءة ملف GenBank

```python
from Bio import SeqIO

# GenBank يحتوي عادةً على تسلسل واحد مشروح بالكامل
record = SeqIO.read("NC_000913.gb", "genbank")

# المعلومات العامة
print(f"المعرف: {record.id}")
print(f"الوصف: {record.description}")
print(f"الكائن الحي: {record.annotations['organism']}")
print(f"طول التسلسل: {len(record.seq):,} bp")
```

!!! note "parse مقابل read"
    - `SeqIO.parse()` — لملفات تحتوي **عدة** تسلسلات (تُرجع مكرراً)
    - `SeqIO.read()` — لملفات تحتوي **تسلسلاً واحداً** (تُرجع سجلاً واحداً)

#### استكشاف الميزات (Features)

الميزات هي التعليقات التوضيحية — مواقع الجينات، مناطق الترميز، إلخ:

```python
from Bio import SeqIO

record = SeqIO.read("NC_000913.gb", "genbank")

# عد أنواع الميزات
feature_types = {}
for feature in record.features:
    ftype = feature.type
    feature_types[ftype] = feature_types.get(ftype, 0) + 1

print("أنواع الميزات:")
for ftype, count in sorted(feature_types.items()):
    print(f"  {ftype}: {count}")
```

**استخراج معلومات الجينات:**

```python
from Bio import SeqIO

record = SeqIO.read("NC_000913.gb", "genbank")

# البحث عن تسلسلات الترميز (CDS)
gene_count = 0
for feature in record.features:
    if feature.type == "CDS":
        gene_count += 1

        # استخراج اسم الجين
        gene_name = feature.qualifiers.get('gene', ['غير معروف'])[0]

        # استخراج الموقع
        location = feature.location

        # استخراج المنتج البروتيني
        product = feature.qualifiers.get('product', ['غير معروف'])[0]

        # طباعة أول 5 جينات فقط كمثال
        if gene_count <= 5:
            print(f"الجين: {gene_name}")
            print(f"  الموقع: {location}")
            print(f"  المنتج: {product}")
            print()

print(f"\nإجمالي تسلسلات الترميز: {gene_count}")
```

---

## 5. الوصول البرمجي إلى NCBI

### لماذا الوصول البرمجي؟

موقع NCBI على الإنترنت ممتاز للبحث اليدوي عن تسلسل أو اثنين. لكن ماذا لو أردت:

- تنزيل تسلسلات 100 جين؟
- البحث عن جميع الأبحاث المنشورة عن CRISPR في آخر سنة؟
- جلب معلومات عن عدة كائنات حية ومقارنتها؟

**Entrez** هو واجهة برمجة NCBI التي تتيح لك فعل كل هذا من خلال Python بدلاً من المتصفح.

### 5.1 الإعداد والقواعد

```python
from Bio import Entrez

# ضبط البريد الإلكتروني (مطلوب من NCBI)
Entrez.email = "your.email@example.com"
```

!!! warning "قواعد استخدام NCBI — مهمة جداً"
    NCBI خدمة مجانية لكنها ليست بلا حدود:

    - **سجل بريدك الإلكتروني** — NCBI تتواصل معك إذا كان هناك مشكلة
    - **حد أقصى 3 طلبات في الثانية** — لا تُغرق الخادم
    - **خزّن النتائج محلياً** — لا تعيد تنزيل نفس البيانات
    - **استخدم التنزيل كمجموعات** — اطلب عدة تسلسلات في طلب واحد بدلاً من طلب لكل تسلسل

### 5.2 البحث في قواعد البيانات

#### البحث في PubMed (الأبحاث العلمية)

```python
from Bio import Entrez

Entrez.email = "your.email@example.com"

# البحث عن مقالات عن CRISPR
handle = Entrez.esearch(
    db="pubmed",           # قاعدة البيانات
    term="CRISPR",         # مصطلح البحث
    retmax=5               # الحد الأقصى للنتائج
)
results = Entrez.read(handle)
handle.close()

print(f"عدد النتائج الإجمالي: {results['Count']}")
print(f"المعرفات المُرجعة: {results['IdList']}")
```

خطوات العملية:

1. `Entrez.esearch()` يُرسل استعلام البحث إلى NCBI
2. `Entrez.read()` يقرأ الاستجابة ويحولها إلى بيانات Python
3. `handle.close()` يغلق الاتصال (مهم لتحرير الموارد)

#### البحث في قاعدة النيوكليوتيدات

```python
from Bio import Entrez

Entrez.email = "your.email@example.com"

# البحث عن تسلسلات BRCA1 البشرية
handle = Entrez.esearch(
    db="nucleotide",
    term="BRCA1[Gene] AND Homo sapiens[Organism]",
    retmax=5
)
results = Entrez.read(handle)
handle.close()

print(f"عدد تسلسلات BRCA1 البشرية: {results['Count']}")
for seq_id in results['IdList']:
    print(f"  معرف: {seq_id}")
```

!!! tip "نصائح البحث"
    يمكنك تحسين البحث باستخدام حقول محددة:

    - `[Gene]` — البحث في أسماء الجينات
    - `[Organism]` — تحديد الكائن الحي
    - `[PDAT]` — تاريخ النشر (مثل `2024[PDAT]`)
    - `AND` / `OR` / `NOT` — دمج شروط البحث

### 5.3 جلب السجلات

#### تنزيل تسلسل واحد

```python
from Bio import Entrez, SeqIO

Entrez.email = "your.email@example.com"

# جلب سجل GenBank لـ BRCA1 mRNA
handle = Entrez.efetch(
    db="nucleotide",
    id="NM_007294",      # معرف التسلسل
    rettype="gb",        # التنسيق المطلوب (GenBank)
    retmode="text"       # نص عادي
)

# تحليل كسجل GenBank
record = SeqIO.read(handle, "genbank")
handle.close()

print(f"المعرف: {record.id}")
print(f"الوصف: {record.description}")
print(f"الطول: {len(record.seq)} bp")
print(f"الكائن: {record.annotations['organism']}")

# حفظ محلياً لتجنب التنزيل مرة أخرى
SeqIO.write(record, "BRCA1.gb", "genbank")
print("تم الحفظ في BRCA1.gb")
```

#### التنزيل كمجموعات: عدة تسلسلات معاً

```python
from Bio import Entrez, SeqIO

Entrez.email = "your.email@example.com"

def download_sequences(id_list, output_file):
    """
    تنزيل عدة تسلسلات من NCBI وحفظها في ملف FASTA.

    استخدام طلب واحد للتنزيل الدفعي أفضل بكثير من
    إرسال طلب منفصل لكل تسلسل (أسرع وأقل حملاً على الخادم).
    """
    # جلب جميع التسلسلات في طلب واحد
    handle = Entrez.efetch(
        db="nucleotide",
        id=id_list,          # قائمة المعرفات
        rettype="fasta",
        retmode="text"
    )

    # القراءة والحفظ
    records = list(SeqIO.parse(handle, "fasta"))
    handle.close()

    SeqIO.write(records, output_file, "fasta")
    print(f"تم تنزيل {len(records)} تسلسل إلى {output_file}")

    # عرض ملخص
    for record in records:
        print(f"  {record.id}: {len(record.seq)} bp")

# تنزيل ثلاثة جينات مهمة
gene_ids = ["NM_007294", "NM_000546", "NM_001904"]
download_sequences(gene_ids, "important_genes.fasta")
```

### 5.4 سير عمل كامل: من البحث إلى التحليل

لنجمع كل ما تعلمناه في مثال واقعي:

```python
from Bio import Entrez, SeqIO

Entrez.email = "your.email@example.com"

def search_and_analyze(gene_name, organism="Homo sapiens"):
    """
    البحث عن جين في NCBI، تنزيل التسلسل، وتحليله.

    Args:
        gene_name: اسم الجين (مثل "BRCA1")
        organism: الكائن الحي (افتراضي: الإنسان)
    """
    print(f"\n🔍 البحث عن {gene_name} في {organism}...")

    # الخطوة 1: البحث
    handle = Entrez.esearch(
        db="nucleotide",
        term=f"{gene_name}[Gene] AND {organism}[Organism] AND mRNA[Filter]",
        retmax=1
    )
    results = Entrez.read(handle)
    handle.close()

    if not results['IdList']:
        print("  لم يُعثر على نتائج!")
        return

    seq_id = results['IdList'][0]
    print(f"  تم العثور على المعرف: {seq_id}")

    # الخطوة 2: الجلب
    handle = Entrez.efetch(
        db="nucleotide",
        id=seq_id,
        rettype="gb",
        retmode="text"
    )
    record = SeqIO.read(handle, "genbank")
    handle.close()

    # الخطوة 3: التحليل
    seq = str(record.seq)
    gc = (seq.count('G') + seq.count('C')) / len(seq) * 100

    print(f"  الوصف: {record.description}")
    print(f"  الطول: {len(seq):,} bp")
    print(f"  محتوى GC: {gc:.1f}%")

    # عد ميزات CDS
    cds_count = sum(1 for f in record.features if f.type == "CDS")
    print(f"  تسلسلات الترميز: {cds_count}")

# تحليل عدة جينات
for gene in ["BRCA1", "TP53", "EGFR"]:
    search_and_analyze(gene)
```

---

## 6. أفضل الممارسات

### لماذا تهمنا "أفضل الممارسات"؟

يمكنك كتابة برنامج يعمل لكنه فوضوي — ستعمل النتائج الآن، لكن بعد شهر لن تفهم ما كتبته. أو يأتي زميل ولا يستطيع استخدام كودك. **أفضل الممارسات** تجعل كودك:

- **مقروء** — ستشكر نفسك في المستقبل  :)
- **قابل لإعادة الاستخدام** — اكتب مرة، استخدم مراراً
- **موثوق** — يعمل بشكل صحيح ويمكن التحقق منه

### 6.1 تنظيم النص البرمجي

```python
"""
analyze_sequences.py - تحليل محتوى GC لتسلسلات FASTA

الاستخدام:
    python analyze_sequences.py input.fasta output.csv
"""

from Bio import SeqIO
import sys


def calculate_gc(sequence):
    """حساب نسبة محتوى GC لتسلسل."""
    gc_count = sequence.count('G') + sequence.count('C')
    return (gc_count / len(sequence)) * 100


def analyze_fasta(input_file, output_file):
    """
    تحليل محتوى GC لجميع التسلسلات وحفظ النتائج.

    Args:
        input_file: مسار ملف FASTA
        output_file: مسار ملف CSV للنتائج
    """
    results = []

    for record in SeqIO.parse(input_file, "fasta"):
        gc = calculate_gc(str(record.seq))
        results.append(f"{record.id},{len(record.seq)},{gc:.2f}")

    with open(output_file, 'w') as f:
        f.write("seq_id,length,gc_content\n")
        for line in results:
            f.write(line + "\n")

    print(f"تم تحليل {len(results)} تسلسل → {output_file}")


def main():
    """نقطة الدخول الرئيسية."""
    if len(sys.argv) != 3:
        print("الاستخدام: python analyze_sequences.py input.fasta output.csv")
        sys.exit(1)

    analyze_fasta(sys.argv[1], sys.argv[2])


if __name__ == "__main__":
    main()
```

**لماذا هذا الهيكل؟**

- **السطر الأول** (docstring): يشرح ماذا يفعل البرنامج وكيف تستخدمه
- **الاستيرادات**: كل المكتبات في البداية
- **الدوال**: كل مهمة في دالة منفصلة
- **`main()`**: نقطة دخول واحدة واضحة
- **`if __name__`**: يسمح باستيراد الدوال دون تشغيل البرنامج تلقائياً

### 6.2 التوثيق الجيد

```python
def align_sequences(seq1, seq2, match=1, mismatch=-1, gap=-2):
    """
    محاذاة زوجية باستخدام خوارزمية Needleman-Wunsch.

    Args:
        seq1 (str): التسلسل الأول
        seq2 (str): التسلسل الثاني
        match (int): درجة التطابق (افتراضي: 1)
        mismatch (int): عقوبة عدم التطابق (افتراضي: -1)
        gap (int): عقوبة الفجوة (افتراضي: -2)

    Returns:
        tuple: (التسلسل المحاذى 1، التسلسل المحاذى 2، الدرجة)

    Example:
        >>> align_sequences("ATGC", "ATGC")
        ('ATGC', 'ATGC', 4)
    """
    # التنفيذ...
```

!!! tip "مستويات التوثيق"
    1. **Docstring للملف** — ماذا يفعل البرنامج ككل
    2. **Docstring للدوال** — ماذا تفعل كل دالة، مدخلاتها ومخرجاتها
    3. **تعليقات سطرية** — لماذا (وليس ماذا) اخترت هذا النهج

    القاعدة: الكود يقول **ماذا**، التعليقات تقول **لماذا**.

### 6.3 اختبار الكود

لا تثق بكودك حتى تختبره. اكتب اختبارات بسيطة للتأكد:

```python
def test_gc_content():
    """اختبار حساب محتوى GC بحالات معروفة."""

    # حالة 1: كل الحروف GC — يجب أن تكون 100%
    assert calculate_gc("GCGCGC") == 100.0

    # حالة 2: لا يوجد GC — يجب أن تكون 0%
    assert calculate_gc("ATATAT") == 0.0

    # حالة 3: نصف ونصف — يجب أن تكون 50%
    result = calculate_gc("ATGC")
    assert result == 50.0

    # حالة 4: تسلسل واقعي
    result = calculate_gc("ATGCGATCG")
    assert 55 < result < 56  # ~55.6%

    print("جميع الاختبارات نجحت!")

test_gc_content()
```

`assert` تتحقق من شرط — إذا كان خاطئاً، يتوقف البرنامج ويخبرك بالخطأ.

### 6.4 الأداء: التعامل مع الملفات الكبيرة

??? note "كود فعال مقابل غير فعال"
    **المشكلة**: ملفات التسلسل قد تحتوي ملايين السجلات. تحميلها كلها في الذاكرة مرة واحدة قد يستهلك ذاكرة الجهاز بالكامل.

    **غير فعال — يحمل كل شيء في الذاكرة:**
    ```python
    # ❌ خطير مع الملفات الكبيرة
    all_records = list(SeqIO.parse("huge_file.fasta", "fasta"))
    for record in all_records:
        process(record)
    ```

    **فعال — يعالج تسلسلاً تلو الآخر:**
    ```python
    # ✓ آمن — يحمل سجلاً واحداً في كل مرة
    for record in SeqIO.parse("huge_file.fasta", "fasta"):
        process(record)
    ```

    الفرق: `SeqIO.parse()` تُرجع **مكرراً** (iterator) — تقرأ سجلاً واحداً في كل دورة وتتخلص منه قبل قراءة التالي. لكن عندما تحولها إلى قائمة بـ `list()`، تضع كل شيء في الذاكرة.

---

## التمارين

### التمرين 1: إحصائيات التسلسل

اكتب دالة `sequence_stats()` تحسب إحصائيات شاملة لتسلسل DNA:

```python
def sequence_stats(dna_seq):
    """
    حساب الإحصائيات لتسلسل DNA.

    يجب أن تُرجع قاموساً يحتوي على:
    - length
    - gc_content (%)
    - at_content (%)
    - nucleotide_counts (dict)
    """
    # كودك هنا
    pass

# اختبار
seq = "ATGCGATCGTAGCTAGCT"
stats = sequence_stats(seq)
print(stats)
# الإخراج المتوقع:
# {
#   'length': 18,
#   'gc_content': 55.56,
#   'at_content': 44.44,
#   'nucleotide_counts': {'A': 4, 'T': 4, 'G': 6, 'C': 4}
# }
```

### التمرين 2: محلل FASTA

اكتب نصاً برمجياً:

1. يقرأ ملف FASTA
2. يحسب محتوى GC لكل تسلسل
3. يكتب النتائج إلى ملف CSV بأعمدة: `id,length,gc_content`

### التمرين 3: تنزيل NCBI

اكتب دالة:

1. تبحث في NCBI عن اسم جين
2. تنزل أفضل 5 نتائج
3. تحفظها في ملف FASTA

---

## القراءات

### مطلوب

1. **Python for Biologists** - Martin Jones (الفصول 1-5)
   *التركيز*: أساسيات Python مع أمثلة بيولوجية

2. **Biopython Tutorial and Cookbook**
   *التركيز*: SeqIO، كائنات Seq، Entrez

### تكميلي

3. **Python Documentation** - الأنواع والوظائف المدمجة
4. **NCBI E-utilities Documentation** - مرجع Entrez API

---

## التقييم الذاتي

بعد إكمال هذا الفصل، يجب أن تكون قادراً على:

- [ ] شرح لماذا Python مناسبة للمعلوماتية الحيوية
- [ ] إنشاء واستخدام المتغيرات بأنواعها الأربعة (نص، عدد صحيح، عشري، منطقي)
- [ ] التعامل مع النصوص: التقطيع، العد، الاستبدال
- [ ] استخدام القوائم لتخزين ومعالجة مجموعات البيانات
- [ ] استخدام القواميس لربط المعلومات
- [ ] كتابة شروط لاتخاذ قرارات بناءً على البيانات
- [ ] كتابة حلقات لتكرار العمليات على مجموعات
- [ ] تعريف واستخدام الدوال لتنظيم الكود
- [ ] استخدام كائن Seq من Biopython
- [ ] قراءة وتحليل ملفات FASTA و FASTQ و GenBank
- [ ] الوصول إلى NCBI والبحث وتنزيل التسلسلات
- [ ] كتابة كود منظم وموثق وقابل للاختبار

!!! tip "الممارسة تصنع الإتقان"
    الطريقة الوحيدة لتعلم البرمجة هي بكتابة الكود. أكمل جميع التمارين وجرب بياناتك البيولوجية الخاصة.

---

## الارتباط بالفصول المستقبلية

!!! info "ما بعد هذا الفصل"
    **الفصل الرابع** (قواعد البيانات) سيبني على:

    - مهارات Entrez التي تعلمتها هنا
    - تحليل الملفات بتنسيقات مختلفة
    - هياكل البيانات لتخزين النتائج

    **الفصل الخامس** (محاذاة التسلسلات) سيستخدم:

    - معالجة التسلسلات (النصوص وكائنات Seq)
    - قراءة/كتابة الملفات
    - كتابة الدوال لتنفيذ الخوارزميات

    **الفصل السادس** (الإحصاء) سيحتاج:

    - القوائم والقواميس لتنظيم البيانات
    - الحلقات لمعالجة مجموعات كبيرة
    - Python كأداة حوسبة إحصائية

**الفصل التالي**: [الفصل الرابع: قواعد البيانات البيولوجية](module-4.md) — الآن بعد أن أصبحت قادراً على البرمجة، ستتعلم الوصول إلى البيانات ودمجها من المستودعات العامة.

---

[↑ فهرس المقرر](index.md) | [← الفصل الثاني](module-2.md) | [التالي: الفصل الرابع →](module-4.md)
